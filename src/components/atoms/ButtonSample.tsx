import ThemeContext from "@/app/use-context-sample/themeContext";
import { useContext } from "react";

export default function ButtonSample() {
  // const theme = useContext(ThemeContext);

  return (
    <>
      <h3>This is button sample</h3>
      {/* <p>theme is {theme}</p> */}
    </>
  );
}
